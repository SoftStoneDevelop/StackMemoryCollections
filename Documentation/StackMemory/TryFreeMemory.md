# TryFreeMemory(nuint) Method

## Definition
Try Frees up occupied memory.

```C#
public bool TryFreeMemory(nuint reducingBytes)
```

## Parameters
`reducingBytes nuint`

Freed memory size in bytes

## Returns
`bool`

`False` if If all memory is already free ohterwise `True`
